experiment_id: "p4-metaharness-mini-qwen"
description: "Qwen3.5-397B solver + Claude Opus 4.6 proposer, Meta-Harness evolution, SWE-bench Verified Mini 50 tasks"
phase: 4

# Task / Dataset
model_id: "qwen/qwen3.5-397b-a17b"
dataset: "MariusHobbhahn/swe-bench-verified-mini"
limit: 50

# Solver agent config (same as baseline v2 for fair comparison)
max_steps: 140
window_size: 120
max_tokens: 65536
efficiency_prompt: false

# Evolution config
algorithm: "meta_harness"
batch_size: 50           # full benchmark per eval cycle
max_cycles: 3            # 3 rounds for quick validation, --resume to continue
holdout_ratio: 0.0       # no holdout, search + eval on same tasks
evolve_prompts: true
evolve_skills: true
evolve_memory: true
evolve_tools: true
egl_threshold: 0.02
egl_window: 3

# MetaHarnessEngine extras — proposer is always Opus 4.6
harness_enabled: true
proposer_model: "anthropic/claude-opus-4.6"
proposer_api_base: "https://api.miromind.site"
proposer_max_turns: 200
proposer_timeout_sec: 3600
num_candidates: 2
eval_sample_size: 0       # 0 = all 50 tasks per candidate eval
rollback_on_regression: false

# Solver / eval
parallel: 25              # parallel solve workers
max_retries: 5

# Workspace
seed_workspace: "seed_workspaces/swe"
output_dir: "experiments/qwen35-swe/logs/p4-metaharness-mini"
